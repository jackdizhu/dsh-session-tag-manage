# openspec/config.yaml
schema: spec-driven

context: |
  技术栈：
    - 宿主框架：DeepSeek Harness (DSH) 插件系统（一切皆插件）
    - 语言：TypeScript 5（ESM，package.json type: module）
    - 插件框架：Cordis（@deepseek-ai/cordis），ctx.effect 生命周期托管
    - 配置 Schema：Schemastery（@deepseek-ai/schemastery）
    - 客户端：React + 客户端槽位（slots / clientRuntime / SessionStandardProps）
    - 类型化 RPC：Typert RPC（构建时生成客户端桩 + 宿主服务桩）
    - 状态投影：ctx.sessionProjections（stateSchema + wire:{viewSchema,view} 契约）
    - 事件系统：SessionEvent 日志，经声明合并扩展 SessionEventMap
    - 测试框架：待配置
  开发运行方式：
    - 本地开发：cordis.yml + `pnpm dsh web --patch <绝对路径>/cordis.yml`
    - 分发：package.json 声明 dsh.bundle.patch（宿主）+ dsh.client（浏览器端）
  API 风格：事件驱动（session/event 消费）+ 投影（session/projection 推帧）+ Typert RPC（客户端→宿主写通路）
  关键领域约定：
    - 会话标签五分类：in_progress / abnormal_end / waiting / completed / invalid
    - 判定策略：规则前置 + LLM 兜底（ctx.llm.stream + BlockAssembler 组装，JSON 约束输出）
    - 标签持久化：session.append('session-tag/assigned', ignorable: true) 事件，随日志回放/Fork/恢复一致
    - 客户端渲染：CSS 类名定位（sidebar.workspaces 单槽、无逐会话行槽位），插件自有 class + 全局样式
    - 手动标签：Typert RPC 写通路 + 投影"后写覆盖" + 锁定手动标签（source === 'user-override' 时自动分析不覆盖）
    - 每日 17:00 提醒：客户端本地汇总 + 聚焦兜底（Web Notifications API）
  项目结构：
    - src/index.ts: 宿主侧入口（apply(ctx, config)，事件监听与计时编排）
    - src/config.ts: 配置 Schema（Schemastery）
    - src/events.ts: 自定义 SessionEventMap 声明合并（session-tag/assigned）
    - src/tagger.ts: 核心逻辑（计时 + 内容提取 + LLM 兜底判定）
    - src/rules.ts: 规则判定器（纯函数）：abnormal / waiting / todo 推断
    - src/projection.ts: SessionProjection 注册（纯同步 fold + Zod schema）
    - src/override.ts: 宿主侧 Typert RPC 服务（sessionTagOverride.set）：校验 + 追加 user-override 事件
    - src/client/index.ts: 客户端插件（背景色渲染：CSS 类名定位 + useProjection）
    - src/client/reminder.ts: 客户端插件（每日 17:00 会话梳理桌面提醒）
    - src/client/tagEditor.tsx: 客户端插件（会话标签手动编辑组件，Typert RPC 调用）
    - docs/: 设计文档（design.md 为设计稿与官方 API 核对依据）

rules:
  workflow:
    - 探索模式 (explore) → 提案模式 (propose) → 执行模式 (apply-change)；
    - 探索过程中涉及不确定的点(需求、UI交互、功能点细节、上下文依赖、技术栈选择、公共组件、公共库等)，必须列出待确认清单，禁止假设；
    - 探索过程中，列出待确认清单后，优先进行深度思考，给出对应的解决方案(从长期架构、可维护性、可扩展性、运行稳定性角度考虑)；
    - 探索结束后，必须先退出探索模式，创建提案，禁止直接执行；
    - 提案必须经用户确认后，才能进入执行阶段；
  tasks:
    - 每个任务最大 2 小时
    - 禁止在提案确认前创建或执行任务
    - 属于文件变更的任务，必须包含变更文件路径和完整变更内容（diff 新增/删除/修改 行形式呈现）；
    - |
      执行已确定的任务时，禁止不必要的提问，必须完整执行所有任务：
      1. 提案确认后，进入执行阶段，禁止中途暂停询问是否继续；
      2. 遇到阻塞问题时，优先尝试自主解决，仅在无法解决时才提问；
      3. 执行过程中不汇报进度，仅在完成或遇到严重问题时汇报；
      4. 完整执行所有任务，包括单元测试、验证、审计等后续任务；
      5. 执行完成后，一次性汇报所有任务完成情况。
    - |
      tasks 只输出一个文件（tasks.md），规范如下：
      1. tasks.md 使用 checkbox 格式列出所有任务状态（让 openspec 工具能够识别）；
      2. tasks.md 的最后一个任务必须为「sub-agent 任务审计任务」；
      3. 每个 spec 执行完成后，必须立即执行 Sub-agent 任务审计；
      4. 审计发现问题后，必须修复问题并重新审计。

  proposal:
    - 设计提案时，必须包含验证步骤
    - 提案必须包含：设计说明、任务列表、验证方案
  design:
    - 设计过程中，必须考虑项目架构、可维护性、可扩展性、运行稳定性等因素；
    - 复杂场景需要输出`mermaid`格式的序列图、类图、状态图等；
    - 设计必须包含：设计说明、任务列表、验证方案、验证步骤；
  specs:
    - 每个 spec 必须包含：模块归属，便于后续sync执行同步时合并文件；
    - 使用 Given/When/Then 格式
    - 优先参考现有模式而非创造新模式
  sync:
    - 在执行spec同步时，必须按模块同步，多个属于同一个模块的spec必须合并文件；
