{
  "name": "dsh-session-tag-manage",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsdown",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "jsdom": "^30.0.1",
    "tsdown": "^0.11.0",
    "typescript": "^5.7.0",
    "vitest": "^3.2.0"
  }
}
